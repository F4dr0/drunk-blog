<div class=naglowek>Posty</div>

<table>
  <tr>
    <th>Posts:</th>
    <th colspan = "2"></th>

  </tr>

  <% @article.each do |article| %>
  <tr>
    <td><%= article.title %></td>
    <td><%= link_to 'Show', article_path(article)%></td>
  </tr>
  <% end %>
</table>
<%= link_to 'New', new_article_path %>