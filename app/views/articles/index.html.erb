<h1>Chill space</h1>
<%= link_to 'New though', new_article_path%>
<table>
  <tr>
    <th>Thoughts</th>

    <th colspan="2"></th>
    <th></th>
  </tr>

  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title%></td>

      <td><%= link_to 'Show',article_path(article)%></td>
      <td><%= link_to 'Destroy', article_path(article),
                      method: :delete %></td>
    </tr>
  <% end %>
</table>