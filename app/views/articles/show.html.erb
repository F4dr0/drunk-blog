<p>
  <strong>Dude:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<h2>Comments:</h2>
  <%= render @article.comments %>

<h2>new comment</h2>
<%= render 'comments/form' %>

<%= link_to 'Back', controller:'articles' %>
<%= link_to 'Edit', edit_article_path(@article) %>
<%= link_to 'Destroy', article_path(@article),
                method: :delete %>